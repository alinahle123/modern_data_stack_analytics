version: 2

models:
  - name: dim_sellers
    description: "Seller dimension with seller location attributes."

    columns:
      - name: seller_id
        tests:
          - not_null
          - unique