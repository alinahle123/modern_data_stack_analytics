version: 2

models:
  - name: dim_products
    description: "Product dimension with product category information."

    columns:
      - name: product_id
        tests:
          - not_null
          - unique

      - name: product_category_name